---
title: iofogctl delete configmap
---

## iofogctl delete configmap

Delete a ConfigMap

### Synopsis

Delete a ConfigMap from the Controller.

```
iofogctl delete configmap NAME [flags]
```

### Examples

```
iofogctl delete configmap NAME
```

### Options

```
  -h, --help   help for configmap
```

### Options inherited from parent commands

```
      --debug              Toggle for displaying verbose output of API clients (HTTP and SSH)
      --delete-namespace   Also delete the Kubernetes namespace (never deletes "default")
  -n, --namespace string   Namespace to execute respective command within (default "default")
  -v, --verbose            Toggle for displaying verbose output of iofogctl
```

### SEE ALSO

* [iofogctl delete](/iofogctl/cli/iofogctl_delete) - Delete an existing ioFog resource


