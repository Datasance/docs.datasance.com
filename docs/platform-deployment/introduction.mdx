---
title: Platform Deployment Guide - Datasance PoT
description: Learn how to deploy Datasance PoT on remote hosts or Kubernetes clusters. Step-by-step guide for setting up your Edge Compute Network.
keywords: [platform deployment, datasance pot, kubernetes edge, remote deployment, potctl, edge compute network, ecn setup, edgelet, embedded oidc]
---

import { CLI_NAME, PRODUCT_NAME } from '@site/src/config/distribution';

:::info[Authentication first]

PoT **v3.8.0** uses [embedded OIDC](/platform-deployment/embedded-oidc) by default. You do not need Keycloak or an external IdP for a greenfield deploy. Set `auth.mode: embedded` and bootstrap credentials in your Control Plane YAML before you deploy.

:::

:::tip[New to {CLI_NAME}?]

Download and install **{CLI_NAME}** from the [{CLI_NAME} download page](/potctl/download) before continuing.

:::

:::tip[Deployed a local ECN first?]

Try the [Quick Start Guide](/getting-started/quick-start-local) before this guide. It covers the same {CLI_NAME} workflows on a single machine.

:::

# Introduction

An **Edge Compute Network (ECN)** has two main parts: a **Control Plane** (Controller, Router, optional NATS) and **Edgelet nodes** at the edge.

{CLI_NAME} deploys and manages both from YAML manifests. This guide walks through a distributed ECN on remote hosts or a Kubernetes cluster.

For architecture context, see [PoT Architecture](/getting-started/architecture).

## Deployment path

Follow these steps in order:

1. **[Embedded OIDC](/platform-deployment/embedded-oidc)** - Set `auth.mode: embedded` and bootstrap credentials in your Control Plane YAML. For a corporate IdP, see [External OIDC](/platform-deployment/external-oidc).
2. **[Prepare your network](/platform-deployment/prepare-your-network)** - Open firewall ports for Controller, Router, NATS, and EdgeOps Console.
3. **[Prepare remote hosts](/platform-deployment/prepare-your-remote-hosts)** - SSH keys and passwordless sudo for {CLI_NAME}.
4. **Deploy the Control Plane** - Choose [remote](/platform-deployment/remote-control-plane) or [Kubernetes](/platform-deployment/kubernetes-prepare-cluster) ({CLI_NAME} or [Helm](/platform-deployment/kubernetes-helm)).
5. **[Setup Edgelet nodes](/platform-deployment/setup-your-agents)** - Install Edgelet v1.0.0 on edge hosts.

## Deployment options

### Remote Control Plane

Deploy the Controller on one or more Linux hosts over SSH. Prepare the Controller host and each Edgelet node host. See [Remote Control Plane](/platform-deployment/remote-control-plane).

### Kubernetes Control Plane

Deploy the Controller, Operator, Router, and NATS on a Kubernetes cluster. You only need remote hosts for Edgelet nodes. Start at [Kubernetes - Prepare A Cluster](/platform-deployment/kubernetes-prepare-cluster).

## Air-gapped networks

For hosts that cannot reach the internet or container registries, see [Airgap Deployment](/platform-deployment/airgap-deployment). That page covers Edgelet image bundles, multi-arch offline transfer, and [OfflineImage](/yaml-references/reference-offlineimage) for microservice images.

:::tip[Where to go from here?]

Start with [Embedded OIDC Authentication](/platform-deployment/embedded-oidc), then [Prepare your network](/platform-deployment/prepare-your-network).

:::

import EditPageAdmonition from '@site/src/components/EditPageAdmonition';

<EditPageAdmonition />
