---
title: Secret YAML Specification - potctl Reference
description: Reference for Secret YAML specification. Manage sensitive data and credentials securely in Datasance PoT.
keywords: [secrets management, edge security, credentials, potctl, yaml reference, datasance pot, kubernetes secrets]
---

# Secret YAML Specification

`potctl` allows users to deploy and manage secrets.

The secret has a very simple definition

```yaml
apiVersion: datasance.com/v3
kind: Secret
metadata:
  name: test-secret
spec:
  type: Opaque
data:
  username: foo  
  password: bar
```

```yaml
apiVersion: datasance.com/v3
kind: Secret
metadata:
  name: test-secret
spec:
  type: tls
data:
  tls.crt: # base64 encoded string  
```

| Field        | Description                                           |
| ------------ | ----------------------------------------------------- |
| spec.type          | Type of secret Opaque or tls                                      |
| data     | Secret data with key value pairs. If type is tls values must be base64 encoded string                                              |

import EditPageAdmonition from '@site/src/components/EditPageAdmonition';

<EditPageAdmonition />
